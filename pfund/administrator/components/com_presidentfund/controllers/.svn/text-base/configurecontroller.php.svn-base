<?php

//give no direct access
defined( '_JEXEC' ) or die( 'Restricted access' );

jimport('joomla.application.component.controller');

class PFundControllerConfigure extends JController
{
    function display()
    {
        $view = $this->getView('configurelinks', 'html');
        $view->display();
    }
}